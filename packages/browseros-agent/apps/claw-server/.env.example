# Copy to .env.development locally; bun --env-file=.env.development
# loads these before the standalone Claw server starts.

# HTTP port for the standalone Claw API.
# CLAW_SERVER_PORT="9200"

# BrowserOS Chromium DevTools port that Claw-server dials over CDP.
# BROWSEROS_CLAW_CDP_PORT="49337"

# Packaged resources root; migrations are read from <dir>/db/migrations.
# BROWSEROS_CLAW_RESOURCES_DIR="./resources"

# Optional JSON config path. Copy config.sample.json to claw.config.json first.
# CLI --config <path> wins when both are set.
# CLAW_CONFIG="./claw.config.json"
